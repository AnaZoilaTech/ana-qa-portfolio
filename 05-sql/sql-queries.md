# SQL Queries for QA Testing

This file contains sample SQL queries used for QA practice, including SELECT statements, filtering, sorting, and JOIN operations.

---

## 1. Select all users
```sql
SELECT * FROM users;
